#Conexión a la base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/airline

#Configuración de JPA y Hibernate
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none

#Configuración del proyecto
spring.application.name=microservice-user-jwt
eureka.instance.instance-id=${spring.application.name}:${random.uuid}
server.port=0

#FlyWay
spring.flyway.locations=classpath:/db/migration

#Vault
spring.cloud.vault.application-name=microservice-user-jwt
spring.cloud.vault.host=localhost
spring.cloud.vault.port=8200
spring.cloud.vault.scheme=http
spring.cloud.vault.authentication=TOKEN
spring.cloud.vault.token=84928946-7489-3154-0548-816469012645
